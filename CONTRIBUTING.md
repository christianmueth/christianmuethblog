# Contributing

Keep changes focused on the blog experience and preserve the site's clear, editorial tone.

Before opening a pull request:

1. Run `npm run lint`.
2. Run `npm run build`.
3. Verify that responsive layouts remain readable on narrow screens.
4. Avoid introducing references to external products or unrelated applications.
